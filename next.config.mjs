/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "images.pexels.com",
            "securityaffairs.com",
            "cdn.dummyjson.com",
        ]
    }
};

export default nextConfig;
